# Dependencias del proyecto.
#
# Los resultados de la memoria (resultados/metricas/*.json y el modelo
# modelos/modelo_sentimiento.joblib) se generaron con:
#     Python 3.10 · pandas 2.3.3 · numpy 2.2.6 · scipy 1.15.3 · scikit-learn 1.7.2
# Con versiones posteriores el código funciona sin cambios, pero al cargar el
# modelo ya entrenado las probabilidades individuales pueden variar unos
# puntos (por ejemplo, la reseña mixta de la app pasa del 90,9 % con
# scikit-learn 1.7.2 al 96,2 % con 1.9.1) y el recuento de reseñas bajo el
# umbral de alerta puede diferir en unas decenas sobre 1.500. Ninguna
# clasificación ni conclusión cambia; las métricas globales se reproducen
# reentrenando con `python src/modelado.py`.

# Núcleo de análisis
pandas>=2.0
numpy>=1.24
scipy>=1.10

# Modelización
scikit-learn>=1.3
joblib>=1.3

# Visualización
matplotlib>=3.7
seaborn>=0.12
altair>=5.0

# Explicabilidad
lime>=0.2.0.1

# Aplicación (productivización)
streamlit>=1.30
